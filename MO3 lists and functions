#7.4 to 7.7
# 7.4 Create a list with the elements "mozzarella", "cinderella", "salmonella"
things = ["mozzarella", "cinderella", "salmonella"]
print(things)

# 7.5 Capitalize the element referring to a person
things[1] = things[1].capitalize()
print(things)

# 7.6 Make the cheese all uppercase
things[0] = things[0].upper()
print(things)

# 7.7 Delete the disease from the list
things.remove("salmonella")
print(things)


#9.1 to 9.2
# 9.1 Define the function good()
def good():
    return ['Harry', 'Ron', 'Hermione']

# 9.2 Define the generator function get_odds()
def get_odds():
    for number in range(10):
        if number % 2 != 0:
            yield number

# Test good()
print(good())

# Find and print the third value returned by get_odds()
count = 0
for odd in get_odds():
    count += 1
    if count == 3:
        print(odd)
        break
